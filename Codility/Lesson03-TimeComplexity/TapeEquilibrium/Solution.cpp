#include <algorithm>
#include <cmath>

int solution(vector<int> &A) {
    // O(n) 으로 풀 수 있음

    // int n = A.size();
    
    // vector<int> diffs(n);
    
    // for(int i = 0; i < n; i += 1) {
    //     for(int j = 0; j < i; j += 1) {
    //         diffs[j] += A[i];
    //     }
        
    //     for(int j = i; j < n; j += 1) {
    //         diffs[j] -= A[i];
    //     }
    // }

    // for(int i = 0; i < n; i += 1) {
    //     diffs[i] = abs(diffs[i]);
    // }
    
    // sort(diffs.begin(), diffs.end());

    return diffs[0];
}
